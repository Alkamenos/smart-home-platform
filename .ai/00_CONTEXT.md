# AI Context Index

Этот файл — точка входа для ИИ-ассистента. Он описывает структуру harness и правила его использования.

## 🚀 НОВАЯ ТОЧКА ВХОДА

**⚠️ ВАЖНО:** Начни с файла **`00_START_HERE.md`** — там пошаговая инструкция выполнения задачи!

## Как использовать

1. **Прочитать `00_START_HERE.md`** — главный workflow ← НАЧНИ ЗДЕСЬ ✅
2. **Изучить файлы по порядку** (01 → 06) — для контекста
3. **Обновлять файлы** после завершения значимых задач

## Структура файлов

| Файл | Назначение | Частота обновления |
|------|-----------|-------------------|
| `00_START_HERE.md` | **Пошаговая инструкция выполнения задачи** | **Всегда актуален** ✅ |
| `01_PROJECT_STATE.md` | Текущее состояние проекта | После каждой значимой задачи |
| `02_ARCHITECTURE.md` | Архитектурные решения | При изменениях архитектуры |
| `03_ROADMAP.md` | История + планы (машиночитаемый) | После каждой задачи |
| `04_RULES.md` | Жесткие ограничения | Редко, по согласованию |
| `05_WORKFLOW.md` | Детальный алгоритм работы ИИ | Редко |
| `06_QUALITY_CHECKS.md` | Чек-листы качества | При добавлении новых проверок |

## Ключевые принципы

1. **Файлы в `.ai/` — живой документ.** ИИ должна обновлять их после каждой значимой задачи.
2. **Не удалять информацию.** Если что-то устарело — помечать как `[DEPRECATED]`, а не удалять.
3. **Добавлять в Known Issues.** Если ИИ нашла баг или долг — записать в `01_PROJECT_STATE.md`.
4. **Roadmap — единый источник истины.** Все планы и статусы задач — только в `03_ROADMAP.md`.

## Настройка Git и SSH

### Remote репозиторий

Основной remote репозиторий проекта:
```
git@github.com:Alkamenos/smart-home-platform.git
```

### Работа с ветками

- **Основная ветка:** `main`
- Вся разработка ведется в ветке `main`
- Пуш выполняется непосредственно в `main`

### Настройка SSH

**⚠️ ВАЖНО:** Перед началом работы необходимо настроить SSH-доступ к репозиторию.

1. **Установить SSH клиент** (если не установлен):
   ```bash
   # Ubuntu/Debian
   sudo apt-get install openssh-client

   # macOS
   brew install openssh

   # Windows (Git Bash уже включает SSH)
   ```

2. **Настроить SSH ключ из переменной окружения `SSH_KEY`**:
   ```bash
   # Создать файл ключа
   echo "$SSH_KEY" > ~/.ssh/id_rsa

   # Установить правильные права
   chmod 600 ~/.ssh/id_rsa

   # Добавить ключ в ssh-agent
   eval "$(ssh-agent -s)"
   ssh-add ~/.ssh/id_rsa
   ```

3. **Проверить подключение**:
   ```bash
   ssh -T git@github.com
   ```

4. **Добавить remote** (если еще не добавлен):
   ```bash
   git remote add origin git@github.com:Alkamenos/smart-home-platform.git
   ```

## Быстрый старт

**Новая задача?** → Следуй алгоритму из `00_START_HERE.md`.

**Нашла баг?** → Добавь в `01_PROJECT_STATE.md` → Known Issues.

**Завершила фазу?** → Обнови `03_ROADMAP.md` и запусти `python3 .ai/scripts/sync_roadmap.py --auto-update`.
